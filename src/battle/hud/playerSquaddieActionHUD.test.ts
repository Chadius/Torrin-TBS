import { describe, it } from "vitest"

describe("playerSquaddieActionHUD", () => {
    it("Will position itself near the mouse", () => {})
    it("Will render a row of buttons, 1 for move + 1 for each action", () => {})
    it("Will render a second row of buttons containing End Turn", () => {})

    it("Will send a message when the user clicks on an action", () => {})

    it("Will disable a button if the squaddie lacks actions to use it", () => {})
})
